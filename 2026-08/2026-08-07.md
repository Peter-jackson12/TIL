# [TIL] 2026-08-07 - Python 기초(변수·동적타이핑·자료형·형변환·연산자·f-string) & 주피터 노트북 실습 및 Q&A

## 1. 오늘 배운 핵심 내용 (Today I Learned)

### 1-1. Python 변수 선언 원리와 동적 타이핑 (2장 1강)
- **참조 메커니즘 (Reference)**: 파이썬 변수는 데이터를 소유하는 상자가 아니라, 메모리에 먼저 할당된 데이터 객체를 가리키는 **'이름표(Label)'** 역할을 함.
- **명명 규칙**: 스네이크 케이스(`snake_case`) 표준 권장. 숫자로 시작 불가, 공백/키워드(if, for 등) 사용 불가.
- **동적 타이핑 (Dynamic Typing)**: 변수 선언 시 타입을 명시하지 않고, 런타임(실행 시점)에 대입되는 값에 따라 자료형이 자동 결정됨.
- **변수 재할당 & 가비지 컬렉터 (Garbage Collector)**: 재할당 시 변수의 참조 주소가 새로 생성된 객체로 이동하며, 이름표를 잃은 이전 데이터는 가비지 컬렉터가 자동 수거함 (`type()`, `id()` 함수로 주소 확인 가능).
- **주피터 메모리 특성**: 파일 내 모든 셀이 단일 전역 메모리 세션을 공유함 (위아래 배치보다 **'실행된 순서'**가 기준). 셀 마지막 줄에 변수명만 적으면 **Auto-Print** 동작.

### 1-2. 기본 자료형과 형변환 (2장 2강)
- **기본 자료형 (Primitive Types)**: `int`(정수), `float`(실수), `str`(문자열), `bool`(논리형).
- **Truthy & Falsy**: 
  - `False`로 인식: `0`, `0.0`, 빈 문자열 `""`, 빈 리스트 `[]`, `None` (비어있는 상태).
  - `True`로 인식: `0`이 아닌 숫자, 글자가 들어있는 문자열 등 (값이 존재하는 상태).
- **명시적 형변환**: `int()`, `float()`, `str()`, `bool()`. (`int(3.99)` 변환 시 반올림이 아닌 **소수점 이하 버림** 처리되어 `3`이 됨).
- **묵시적 형변환 (Upcasting)**: `10 + 3.5` 연산 시 데이터 손실을 막기 위해 정수가 실수형으로 자동 스케일 업되어 `13.5`가 됨.
- **예외 처리 (`ValueError`)**: `"20세"` 처럼 숫자가 아닌 문자가 섞인 문자열을 숫자로 형변환하려 하면 `ValueError` 발생 ➔ 사전 검증 함수 `str.isdigit()` 활용.

### 1-3. 연산자와 단락 회로 평가 (2장 3강)
- **특수 산술 연산자**: `//` (정수 몫), `%` (나머지), `**` (거듭제곱). 일반 나눗셈 `/`의 결과는 항상 `float` 반환.
- **복합 대입 연산자**: `+=`, `-=`, `*=`, `/=`, `%=` 등. 변수 중복 작성을 배제하여 오타 방지 및 가독성 향상.
- **비교 & 논리 연산자**: `==`, `!=`, `<`, `>`, `and`, `or`, `not`. 문자열 비교 시 사전 순서(유니코드 값) 기준 비교.
- **단락 회로 평가 (Short-Circuit Evaluation)**:
  - `and` 연산: 좌측이 `False`이면 우측 연산을 완전히 생략함 (예: `False and (10/0 > 1)` ➔ `ZeroDivisionError` 미발생하고 `False` 반환).
  - `or` 연산: 좌측이 `True`이면 우측 연산 생략.
- **연산자 우선순위**: 거듭제곱(`**`) ➔ 산술(`*`, `/`, `//`, `%` ➔ `+`, `-`) ➔ 비교 ➔ 논리(`not` ➔ `and` ➔ `or`). 소괄호 `()` 수동 제어 권장.

### 1-4. 문자열 형식화 세대별 비교 & f-string (2장 4강)
- **1세대 (`%` 연산자)**: C언어 `printf` 스타일 (%s, %d, %.1f). 타입 불일치 시 에러 발생.
- **2세대 (`str.format()`)**: 중괄호 `{}` 및 인덱스/키워드 대입. 변수가 많아지면 문장과 `.format()` 사이 거리가 멀어져 가독성 저하.
- **3세대 (`f-string`)**: 현대 표준. `f"문자열 {변수명}"` 형태.
  - **정렬/서식 지시자**: `{price:<12}` (좌측 정렬), `{price:>10,}` (우측 정렬 + 천 단위 쉼표), `{rate:.1f}%` (소수점 자릿수).
  - **디버깅 조절자 (`=`)**: `f"{score=}"` ➔ `score=95` 출력. 변수 이름표(라벨)를 손으로 적지 않아도 이름과 값을 동시에 자동 출력.

---

## 2. 오늘 인상 깊었던 인사이트 & 질문 답변 (Q&A / 실습)

### 2-1. VS Code vs PyCharm 사용률 및 환경 차이
- **글로벌 점유율**: Stack Overflow 조사 기준 **VS Code가 약 74%로 압도적 1위** (PyCharm은 약 15%). 100% 무료, 가벼움, 멀티 언어 통합 지원, 풍부한 확장 프로그램(Extension) 생태계가 원인.
- **PyCharm의 강점**: 오직 파이썬 전용 대형 백엔드/Django 프로젝트에서 수백 개 파일의 안전한 **리팩토링(`Shift+F6`)** 및 통합 DB/도커 툴 일체형 제공.

### 2-2. JSDoc vs Python Docstring vs Markdown 셀 비교
- **JSDoc (`/** ... */`)**: **자바스크립트 전용** 주석 규격.
- **Docstring (`""" ... """`)**: **파이썬 전용** 함수/클래스 명세 주석. **반드시 코드 셀 안**에 적어야 에러 없이 에디터가 마우스 호버 시 **팝업 힌트(Quick Doc)**를 띄워줌.
- **마크다운 셀 (`M`)**: 주석용 `#` 없이 작성하며, 사람이 읽는 전체 보고서/목차/서식 작성용.

### 2-3. 의사 난수(PRNG)와 모듈러 편향 (Modular Bias)
- **`random` 모듈의 한계**: '메르센 트위스터' 알고리즘 기반 **가짜 난수(의사 난수)**임. 시드(Seed)를 알면 미래 난수 예측이 가능하므로 보안/암호화에는 `secrets` 모듈 사용 필수.
- **모듈러 편향**: 가짜 난수 때문이 아니라, **나머지 연산자(`%`)를 써서 범위를 제한할 때 나눗셈이 딱 떨어지지 않아 일부 숫자의 확률이 미세하게 높아지는 수학적 불균형 현상**.

### 2-4. 지저분한 텍스트("20세", "스무살")에서 숫자 정제하기
- **순수 파이썬**: `"".join(char for char in text if char.isdigit())` 사용.
- **정규표현식**: `re.findall(r'\d+', text)` 사용.
- **판다스 전처리**: 한글 나이 매핑 사전(`korean_age_dict`) + 정규표현식 + 사용자 정의 함수를 결합하여 `df['age'].apply(clean_age)` 패턴으로 일괄 정제.

### 2-5. 주피터 `In [n]` 동작 원리 및 팁
- `In [n]`의 `n`은 셀의 개수나 위치가 아니라 **'해당 세션에서 실행 버튼(Shift+Enter)을 누른 누적 횟수'**를 뜻함. 똑같은 셀을 2번 누르면 `In [2]`가 됨.
- 주피터 실행 렉 발생 시 `Restart` (커널 재시작) 또는 VS Code `Reload Window` (`Ctrl+Shift+P` ➔ `Reload Window` ➔ Enter) 활용.

---

## 3. 회고 및 향후 다짐 (Keep / Problem / Try)

- **Keep**:
  - 주피터 환경에서 단축키(`A`, `B`, `M`, `Y`, `DD`, `Shift+Enter`)를 적극 활용하여 실습 문제(2장 1~4강)를 완주한 점.
  - 단순 문법 암기를 넘어 의사 난수 원리, 모듈러 편향, f-string 디버깅 조절자(`=`)의 존재 이유 등 깊이 있는 원리를 탐구한 점.
- **Problem**:
  - 파이썬 최초 실행 시 백그라운드 패키지 스캔 및 소켓 통신 지연으로 인해 간단한 사칙연산 셀이 24초 동안 무한 로딩되는 현상에 당황했음.
  - 파이참의 단축키(`Ctrl+Y`)와 VS Code의 기본 단축키 차이로 인해 편집 조작 시 손이 꼬이는 문제 발생.
- **Try**:
  - VS Code에서 `Alt+Shift+Down` (줄 복사), `Alt+Down` (줄 이동), `Ctrl+/` (주석), `Ctrl+D` (동일 단어 멀티 선택) 등 필수 단축키를 손가락 근육에 익히기.
  - 실무 스타일의 영수증 출력 코드나 데이터 정제 로직 작성 시 f-string(`:,`, `:.1f`)을 표준으로 적극 적용하기.

---

## 4. 참고 자료 및 출처

- [Peter-jackson12 GitHub TIL 저장소](https://github.com/Peter-jackson12/TIL)
- [스파르타 내일배움캠프](https://nbcamp.spartacodingclub.kr/)