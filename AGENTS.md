# TIL 저장소 작업 지침

이 저장소는 학습 기록(TIL/Devlog)과 자동 생성 목차를 관리한다.
일반 ChatGPT + GitHub 플러그인으로 문서 작업을 직접 수행하는 것을 기본으로 한다.

## 시작할 때

1. README.md를 읽어 작성 원칙과 목차 생성 규칙을 확인한다.
2. 같은 날짜의 TIL 파일이 이미 있는지 확인한다.
3. 필요한 경우 template.md를 참고한다.
4. 자동 생성 파일은 직접 편집하지 않는다.

## 작성 원칙

- 사용자가 실제로 배운 내용, 질문, 이해한 답을 중심으로 작성한다.
- 직접 실행한 결과와 AI가 제안한 예시를 구분한다.
- 제공되지 않은 실험, 성능 수치, 오류 원인, 개인적 소감을 만들어 넣지 않는다.
- 관찰과 추측을 구분하고 근거가 없으면 미확인으로 남긴다.
- 같은 날짜 파일이 있으면 새 파일을 만들지 말고 기존 내용을 보완한다.
- 머리말의 날짜·단계·주제는 목차 생성의 원본이므로 비워 두지 않는다.
- 단계는 `Stats / ML / Quant`처럼 사선으로 구분할 수 있다.
- 주제는 그날을 한 줄로 설명하고 목차에 그대로 실릴 수 있게 작성한다.

## 자동 생성 파일

다음 파일/구간은 `tools/build_index.py`가 생성하므로 직접 고치지 않는다.

- README.md의 목차 표시 구간
- `YYYY-MM/README.md`
- `docs/topics.md`

TIL 파일 작성 또는 수정 후 목차를 갱신한다.

```powershell
python tools/build_index.py
```

정합성 확인:

```powershell
python tools/build_index.py --check
```

## Git 작업

- 기본 브랜치는 `main`이다.
- 간단한 문서 추가·수정은 GitHub에서 직접 수행할 수 있다.
- 여러 파일이 바뀌거나 자동 목차가 함께 갱신되는 작업은 브랜치 + PR을 우선 고려한다.
- 강제 push/reset은 하지 않는다.
- 기존 기록의 의미를 임의로 다시 쓰지 않는다.
- 작업 완료 보고에는 작성/수정한 TIL, 목차 갱신 여부, 자동 검사 결과를 구분해 적는다.

## Work / 로컬 도구 사용

- Markdown 작성·수정, 인덱스 확인, README 정리는 기본적으로 Work로 넘기지 않는다.
- 일반 ChatGPT + GitHub 플러그인으로 처리한다.
- 로컬 파일, 실행 결과, 다른 프로젝트의 Git 비추적 데이터가 반드시 필요할 때만 별도 로컬 작업을 고려한다.
