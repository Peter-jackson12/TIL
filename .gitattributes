# 저장소에는 항상 LF로 저장한다. 윈도우에서 편집해 CRLF로 저장되더라도
# 커밋 시점에 LF로 정규화되므로, 내용이 같은데 전체 파일이 바뀐 것처럼 보이지 않는다.
* text=auto eol=lf

# 아래는 텍스트가 아니므로 변환하지 않는다.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.xlsx binary
*.zip binary
