# [TIL] 2026-08-06 - Git 심화(브랜치·병합·Rebase·충돌) & Python uv 모던 환경 구축 및 주피터 노트북 연동

## 1. 오늘 배운 핵심 내용 (Today I Learned)

### 1-1. Git 브랜치 관리 및 고급 병합 전략 (Fast-forward, 3-way, Rebase)
- **브랜치의 본질**: 브랜치는 파일 복사본이 아니라 특정 커밋을 가리키는 40바이트의 가벼운 **포인터(Pointer)**이며, `HEAD`는 현재 작업 중인 브랜치/커밋을 가리킴.
- **브랜치 전환 전용 명령어**: 기존 `git checkout`의 다중 책임을 분리하여 Git 2.23+ 버전부터 도입된 **`git switch`** 사용 권장.
- **병합 전략 비교**:
  - **Fast-forward Merge**: 분기가 없을 때 커밋 포인터만 앞으로 이동 (병합 커밋 생성 X).
  - **3-way Merge**: 공통 조상(Base) 이후 양쪽 브랜치 모두 커밋이 생겼을 때 발생하며, **병합 커밋(Merge Commit)**이 자동 생성됨.
  - **주요 옵션**: `--no-ff` (Fast-forward 조건에서도 강제로 병합 커밋 생성하여 흔적 남김), `--squash` (여러 커밋을 하나로 압축 병합).
- **Rebase (선형 이력 만들기)**:
  - 브랜치의 시작점(Base)을 다른 커밋 위로 재설정하여 깔끔한 **일직선(선형) 히스토리**를 생성함 (커밋 해시 재발행).
  - **Rebase의 황금률**: **"이미 원격 저장소에 공유된(Push된) 커밋은 절대 Rebase 하지 않는다!"**
- **충돌(Conflict) 해결**:
  - 두 브랜치가 같은 파일의 같은 줄을 다르게 수정했을 때 발생하며, 에러가 아닌 정상적인 판단 신호임.
  - **충돌 마커 해독**: `<<<<<<< HEAD` (현재 내용) | `=======` (구분선) | `>>>>>>> feature` (병합할 내용).
  - 마커를 수동 제거 후 `git add` ➔ `git commit`으로 완료. Rebase 중 충돌 시 `git rebase --continue` 또는 `git rebase --abort` 사용.

### 1-2. 코드 문서화 및 타입 힌트 (JSDoc & Python Docstrings)
- **JSDoc (`/** ... */`)**: 매개변수(`@param`), 반환값(`@returns`), 권장 안 함(`@deprecated`) 등을 명시하여 IDE 호버 힌트 및 자동완성을 제공함.
- **Python Docstring (`"""`)**: 파이썬 함수 하단에 큰따옴표 3개(`"""`) 입력 시 PyCharm/VS Code가 파라미터 및 타입 명세 틀을 자동 완성해 줌.

### 1-3. Python 모던 툴체인 `uv` 기반 환경 구축
- **가상환경 격리**: 전역(Global) 공간 오염에 따른 **'버전 지옥(Dependency Hell)'**을 방지하기 위해 프로젝트별 독립 공간(`.venv`) 필수 사용.
- **`uv` 툴체인의 특징**: Rust 기반으로 기존 `pip` 대비 10~100배 빠른 속도 제공. 파이썬 버전 관리부터 패키지 고정까지 단일 도구로 통합.
- **핵심 명령어 및 명세서**:
  - `uv init`: 프로젝트 초기화 ➔ 공식 표준 명세서 **`pyproject.toml`** (PEP 621) 및 `.python-version` 생성.
  - `uv run main.py`: 가상환경(`.venv`) 미존재 시 자동 생성 및 실행을 원스톱 처리.
- **실행 방식 비교**: REPL (실시간 피드백/휘발성) vs `.py` 스크립트 (파일 저장/영속성).

### 1-4. VS Code와 주피터 노트북 (.ipynb) 커널 연동
- **주피터 노트북 (`.ipynb`)**: REPL의 즉시성과 스크립트의 영속성을 결합한 하이브리드 포맷.
- **커널 연동**: `uv add ipykernel`로 가상환경 내부에 통신 엔진을 설치한 후, VS Code 우측 상단 `Select Kernel`에서 해당 프로젝트의 `.venv` 인터프리터를 바인딩함.
- **셀 제어 및 단축키**:
  - **코드 셀(Y)**: 파이썬 소스 코드 실행 | **마크다운 셀(M)**: 문서화 작성.
  - **주요 단축키**: `A` (상단 셀 생성), `B` (하단 셀 생성), `Shift + Enter` (실행 후 다음 셀 이동).
- **`In [n]` 비순차 실행의 주의점**: 실행 번호는 위치가 아닌 **누적 실행 횟수**이므로 무작위 실행 시 전역 메모리가 꼬일 수 있음 ➔ 에러 시 `Restart` 후 위에서부터 순차 재실행.

---

## 2. 오늘 인상 깊었던 인사이트 & 배운 점

- **Merge vs Rebase의 시각적 차이**: `git merge`만 계속 사용하면 가지가 옆으로 뻗었다 합쳐지는 '지하철 노선도' 모양이 되지만, `git rebase`를 활용하면 가지의 뿌리를 잘라 메인 끝에 떼어 붙이므로 '완벽한 일직선 기차' 히스토리가 됨을 시각적으로 명확히 이해함.
- **`uv`를 통한 모던 파이썬 생태계 체감**: 기존 `pyenv`, `venv`, `pip`, `requirements.txt`로 파편화되었던 개발 환경이 `uv` 단 하나와 `pyproject.toml` 표준 명세서로 통합되어 초고속으로 관리되는 이점을 직접 경험함.
- **VS Code와 가상환경 연동의 원리**: VS Code에서 `pip` 모듈이 없다고 나오는 경고 로그(`No module named pip`)는 에러가 아니며, 설정(`Python: Always Use Uv`)을 통해 최신 `uv` 도구 기반으로 깔끔하게 매핑시킬 수 있음을 깨달음.

---

## 3. 회고 및 향후 다짐 (KPT)

- **Keep**: 단순 이론 수강에 그치지 않고, 윈도우 파워쉘 및 VS Code 내부 터미널 환경에서 `uv` 설치, PATH 환경 변수 등록, 가상환경 빌드까지 직접 손으로 검증하며 실습한 점.
- **Problem**: 터미널 실행 경로가 `C:\Windows\System32`에 위치했을 때 권한 거부(`UnauthorizedAccess`) 에러가 발생하거나, 새로 세팅한 환경 변수가 VS Code 프로세스 메모리에 즉시 반영되지 않아 명령어 미인식 문제로 당황했음.
- **Try**: 파이참과 VS Code 환경 모두에 익숙해지도록, 오늘 작성한 TIL 마크다운 노트를 **VS Code의 '소스 제어(Source Control)' 탭** 및 브랜치 명령어(`git switch -c feature/...`)를 활용하여 GitHub에 직접 Commit & Push 하고 의도적 충돌(Conflict) 해결 연습까지 완료하기.

---

## 4. 참고 자료 및 출처

- [Peter-jackson12 GitHub TIL 저장소](https://github.com/Peter-jackson12/TIL)
- [스파르타 내일배움캠프](https://nbcamp.spartacodingclub.kr/)