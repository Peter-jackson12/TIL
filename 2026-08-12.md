# 📝 Today I Learned (TIL)

> **날짜:** 2026년 8월 12일  
> **주요 학습 주제:** 파이썬 기초(변수 스코프, 람다, 모듈/패키지, uv 의존성 관리), CS/자료구조(해시테이블 vs B-Tree), 퀀트 백테스팅 아키텍처 리팩토링  
> **사용 개발 환경:** Python 3.14, VS Code, Jupyter Notebook, uv, Git  

---

## 💡 Table of Contents
1. [Python 핵심 문법: 스코프와 람다 함수](#1-python-핵심-문법-스코프와-람다-함수)
2. [모듈과 패키지 & 모듈 섀도잉 주의사항](#2-모듈과-패키지--모듈-섀도잉-주의사항)
3. [현대적 파이썬 의존성 관리: uv 패키지 매니저](#3-현대적-파이썬-의존성-관리-uv-패키지-매니저)
4. [CS & 자료구조: 해시 테이블 vs B-Tree](#4-cs--자료구조-해시-테이블-vs-b-tree)
5. [실무 데이터 파이프라인: 퀀트 백테스팅 엔진 리팩토링](#5-실무-데이터-파이프라인-퀀트-백테스팅-엔진-리팩토링)
6. [개발 환경 & 트러블슈팅 팁 (Q&A)](#6-개발-환경--트러블슈팅-팁-qa)
7. [오늘의 회고 (Retrospective)](#7-오늘의-회고-retrospective)

---

## 1. Python 핵심 문법: 스코프와 람다 함수

### 1.1 LEGB 변수 탐색 규칙
파이썬 인터프리터가 코드 내 변수를 탐색할 때는 안쪽 스코프부터 바깥쪽으로 **L ➔ E ➔ G ➔ B** 순서로 식별합니다.

* **L (Local):** 현재 작동 중인 함수 내부 (우선순위 최고)
* **E (Enclosing):** 나를 감싸고 있는 외부 함수 공간 (중첩 함수 구조)
* **G (Global):** 소스코드 파일 전체 전역 공간
* **B (Built-in):** 파이썬 내장 공간 (`print`, `len` 등)

### 1.2 변수 은닉 (Shadowing)과 `global` 키워드
* **변수 은닉:** 전역 변수와 똑같은 이름으로 함수 내부에 지역 변수를 선언하면, 함수 안에서는 지역 변수가 우선권을 가져 전역 변수가 가려집니다. (전역 데이터 오염 차단)
* **`global` vs `return`:** `global` 키워드는 스코프 꼬임 버그를 유발할 수 있으므로, 함수 간 데이터 전달은 **`return` 기반의 독립적 입력/출력** 설계가 권장됩니다.

```python
# 전역 변수 변경 - return 방식 (권장)
count = 0
def increase_count(c):
    return c + 1

count = increase_count(count)
```

### 1.3 람다(Lambda)와 내장 고차함수 (`map`, `filter`)
일회성 간이 수식을 작성할 때 `lambda`를 다른 고차 함수의 인자로 전달합니다.

* **`map(func, seq)`:** 모든 원소에 동일한 가공 연산 일괄 적용
* **`filter(func, seq)`:** 조건식이 `True`인 알맹이 원소만 선별
* **응용 - 조건부 가공:**
```python
numbers = [1, 2, 3, 4, 5]
# 짝수 데이터만 선별하여 +10 가공
result = list(map(lambda x: x + 10, filter(lambda x: x % 2 == 0, numbers)))
# 결과: [12, 14]
```

---

## 2. 모듈과 패키지 & 모듈 섀도잉 주의사항

### 2.1 모듈과 패키지의 구조
* **모듈 (Module):** 독립된 재사용 단위의 파이썬 파일 (`.py`)
* **패키지 (Package):** 여러 모듈 파일들을 효율적으로 계층화한 폴더 구조 (`__init__.py` 포함)

### 2.2 Import 키워드 및 관례적 별칭 (De Facto Standards)
* `import 모듈명`: 출처 명확 (`math.sqrt()`)
* `from 모듈명 import 기능`: 모듈명 생략 후 호출 (`sqrt()`)
* `import 모듈명 as 별칭`: 관례적 별칭 지정
  * `import pandas as pd`
  * `import numpy as np`
  * `import matplotlib.pyplot as plt`

### ⚠️ 치명적 실습 주의사항: 모듈 섀도잉 (Module Shadowing)
파이썬의 모듈 탐색 1순위는 **현재 작업 중인 디렉터리**입니다. 
따라서 커스텀 `.py` 파일 이름을 `pandas.py`, `random.py`, `math.py`처럼 오픈소스/내장 모듈 이름과 똑같이 지으면, 진짜 라이브러리를 가려버려 `AttributeError`가 발생합니다. (반드시 `my_pandas_test.py` 형태 사용)

---

## 3. 현대적 파이썬 의존성 관리: uv 패키지 매니저

Rust 기반의 **`uv`**는 기존 `pip` 방식의 느린 연산과 의존성 충돌 문제를 완벽히 해결하는 차세대 의존성 관리 도구입니다.

### 3.1 3대 핵심 구성 파일 비교

| 구분 | `pyproject.toml` | `uv.lock` | `.venv` |
| :--- | :--- | :--- | :--- |
| **역할** | 프로젝트 통합 요구 명세서 | 정밀 버전/해시 스냅샷 잠금서 | 로컬 가상환경 바이너리 폴더 |
| **타깃** | 사람이 읽고 작성 | 컴퓨터가 100% 동일 환경 재현 | 현재 내 컴퓨터에서 실행 |
| **Git 공유 여부** | **공유 (O)** | **공유 (O)** | **절대 금지 (X - .gitignore)** |

### 3.2 핵심 명령어 및 워크플로우
```bash
# 1. 특정 파이썬 버전 프로젝트 생성
uv init --python 3.13 my_project

# 2. 패키지 추가 (pyproject.toml 및 uv.lock 즉시 자동 동기화)
uv add pandas matplotlib

# 3. 패키지 제거
uv remove matplotlib

# 4. 유실된 가상환경 고속 복원 (깃허브 클론 후 최우선 실행)
uv sync

# 5. 가상환경 내부 코드 실행
uv run python main.py
```

### 3.3 오픈소스 공급망 공격 (Supply Chain Attack) 방어
PyPI 저장소의 오타 스쿼팅(Typosquatting)이나 개발자 계정 탈취 공격에 대응하여, `uv.lock`은 **SHA-256 고유 해시값**을 기입해 파일의 소스코드 변조를 원천 차단합니다.

---

## 4. CS & 자료구조: 해시 테이블 vs B-Tree

### 4.1 해시 테이블 (Hash Table) - $O(1)$
* **원리:** 수학적 해시 함수를 통해 데이터의 메모리 저장 주소를 즉시 연산합니다.
* **적용:** 파이썬의 `dict`, `set` 자료형 (중복 허용 안 함, 순서 없음).

### 4.2 DB에서 해시 대신 B-Tree($O(\log N)$)를 쓰는 이유
해시 테이블이 $O(1)$로 탐색 속도가 압도적임에도, 관계형 데이터베이스(MySQL, PostgreSQL 등)가 B-Tree 인덱스를 주력으로 사용하는 이유는 다음과 같습니다.

1. **범위 검색 (Range Query) 불가능:** 해시는 키값이 무작위로 분산되므로 `WHERE price BETWEEN 10000 AND 50000` 연산 시 전체 스캔이 필요함. (B-Tree는 리프 노드가 정렬되어 있어 범위 탐색 최적화)
2. **정렬 (`ORDER BY`) 불가능:** B-Tree는 데이터가 항상 정렬 상태를 유지함.
3. **디스크 I/O 최적화:** B-Tree는 16KB 페이지 단위 블록 읽기에 최적화됨.

---

## 5. 실무 데이터 파이프라인: 퀀트 백테스팅 엔진 리팩토링

초봉/호가잔량(LOB) 데이터를 처리하는 단일 파일(600줄+) 백테스팅 엔진을 **모듈화 패키지 구조**로 설계 개선하였습니다.

### 5.1 목표 아키텍처 폴더 구조
```text
Stock/
├── pyproject.toml         # uv 의존성 명세
├── uv.lock                # 버전 동결 잠금 파일
├── REFACTORING_PLAN.md    # 리팩토링 설계 계획서
├── config.py              # 경로 및 전략 파라미터 통합 관리
├── utils.py               # 호가단위, 상한가, 시간차 순수 계산 유틸
├── data_loader.py         # 일봉 CSV 및 SQLite DB 연결 전담
├── risk_manager.py        # 손절/익절/트레일링스탑 전담
├── strategy.py            # 호가잔량 및 윈도우 시그널 계산 전담
├── engine.py              # 백테스팅 핵심 실행 조율기
└── run_parallel.py        # multiprocessing 기반 병렬 실행 및 자동 병합
```

### 5.2 주요 모듈 코드 구현 팁
* **`config.py` - 상대 경로 및 인코딩 통일:**
  ```python
  from pathlib import Path
  BASE_DIR = Path(__file__).resolve().parent
  CSV_PATH = BASE_DIR / "sampledata" / "Daily"
  ENCODING = "utf-8-sig" # 엑셀/Linux/VS Code 한글 깨짐 방지
  ```
* **`run_parallel.py` - 멀티프로세스 병렬 연산 및 자동 병합:**
  ```python
  from multiprocessing import Pool
  import pandas as pd

  if __name__ == '__main__':
      with Pool(processes=12) as pool:
          pool.map(run_worker, tasks)
      
      # 작업 완료 후 CSV 자동 병합
      df_list = [pd.read_csv(f, encoding='utf-8-sig') for f in file_list]
      merged_df = pd.concat(df_list, ignore_index=True)
      merged_df.to_csv("final_result.csv", encoding='utf-8-sig', index=False)
  ```

---

## 6. 개발 환경 & 트러블슈팅 팁 (Q&A)

* **Q1. VS Code 주피터 커널 설정 시 무엇을 선택해야 할까?**
  * ➔ **`Python Environments`**를 선택하고 설치된 파이썬 버전(e.g., Python 3.14 / `.venv`)을 지정해야 실제 계산 엔진이 가동됩니다.
* **Q2. 라이트 모드 vs 다크 모드 시력에 미치는 영향?**
  * ➔ 주변 조명이 밝은 환경에서는 **라이트 모드(양극성 대비)**가 동공을 축소시켜 초점 심도를 깊게 만듭니다. 글자 윤곽이 또렷해지고 읽기 속도가 향상되며 난시의 빛 번짐(Halation Effect)을 방지합니다. (`Ctrl+K, Ctrl+T` 단축키로 변경)
* **Q3. 한글 깨짐 현상 해결책?**
  * ➔ CSV 저장 시 `encoding='utf-8-sig'` 옵션을 사용하면 엑셀과 VS Code 모두에서 한글이 깨지지 않고 정상 표시됩니다.
* **Q4. VS Code 확장 추천:**
  * ➔ **Rainbow CSV**: CSV 파일 열(Column)마다 무지개색 하이라이팅을 제공하여 가독성을 극대화합니다.

---

## 7. 오늘의 회고 (Retrospective)

* **Keep (만족스러운 점):** 단순 문법 습득에 그치지 않고, 기존에 작동시키던 600줄짜리 주식 백테스팅 코드를 실무 수준의 모듈화 아키텍처로 리팩토링하는 기획을 완성했다. `uv`의 현대적 패키지 관리 생태계를 깊이 있게 이해했다.
* **Problem (시행착오/어려웠던 점):** 600MB 규모의 대용량 SQLite DB 파일은 깃허브 단일 파일 제한(100MB)에 걸린다.
* **Try (내일의 시도):** 다음 주 SQL 커리큘럼 이수 직후, SQL 쿼리문(`SELECT`, `WHERE`)을 활용하여 600MB DB를 20MB 수준의 경량 샘플 DB로 정밀 가공하여 `sampledata/`에 탑재하고, `uv sync` 기반 원클릭 실행 깃허브 포트폴리오를 완성할 것이다.
```